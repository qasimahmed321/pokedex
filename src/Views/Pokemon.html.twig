<!DOCTYPE html>
<html lang="en-GB">
    <head>
        <meta charset="utf-8"/>
        <title>{{ name|capitalize }}</title>
        <link rel="stylesheet" href="../CSS/Pokemon.css" type="text/css">

    </head>

    <body>

    {% include 'Templates/nav.html.twig' %}
        <div class="container"><br/>
            <h1 class="text-center">{{ name|capitalize }}</h1>

            <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel" style="width: 100%;">
                <ol class="carousel-indicators">
                    {% for sprite in sprites %}
                        {%  if loop.first %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}" class="active"></li>
                        {%  else %}
                            <li data-target="#carouselExampleIndicators" data-slide-to="{{ loop.index0 }}"></li>
                        {%  endif %}
                    {%  endfor %}
                </ol>

                <div class="carousel-inner">
                    {% for sprite in sprites %}
                        {%  if loop.first %}
                            <div class="carousel-item active">
                                <img class="d-block mx-auto imgfix" src="{{ sprite }}" alt="..." />
                                <div class="carousel-caption d-none d-md-block">
                                    <p>The whole caption will only show up if the screen is at least medium size.</p>
                                </div>
                            </div>
                        {%  else %}
                        <div class="carousel-item">
                            <img class="d-block mx-auto imgfix" src="{{ sprite }}" alt="..."/>
                            <div class="carousel-caption d-none d-md-block">
                                <p>The whole caption will only show up if the screen is at least medium size.</p>
                            </div>
                        </div>
                        {% endif %}
                    {% endfor %}

                </div>
                <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
                    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
                    <span class="carousel-control-next-icon" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>

            <div class="row">
                <div class="col">
                    <h4><span class="font-weight-bold">Height:</span>  {{ height }} </h4>
                    <h4><span class="font-weight-bold">Weight:</span> {{ weight }} kg</h4>
                    <h4><span class="font-weight-bold">Species:</span> {{ species|capitalize }}</h4>

                </div>

                <div class="col">
                    <h4 class="font-weight-bold">Abilities: </h4>
                    <ul>
                        {% for ability in abilities %}
                            {%  if ability['is_hidden'] %}
                                <li>{{ ability['ability']['name']|replace({'-':" "})|capitalize }}</li>
                            {% else %}
                                <li>{{ ability['ability']['name']|replace({'-':" "})|capitalize }} (Hidden Ability)</li>
                            {%  endif %}
                        {%  endfor %}
                    </ul>
                </div>

            </div>

        </div>


    </body>

</html>
